<script src="{{ 'lazysizes.min.js' | asset_url }}" async></script>

{%- if request.page_type contains 'product' -%}
  {{ 'option_selection.js' | shopify_asset_url | script_tag }}
  <script>
    jQuery(function() {
      jQuery('.swatch :radio').change(function() {
        var optionIndex = jQuery(this).closest('.swatch').attr('data-option-index');
        var optionValue = jQuery(this).val();
        jQuery(this)
            .closest('form')
            .find('.single-option-selector')
            .eq(optionIndex)
            .val(optionValue)
            .trigger('change');
      });
    });
  </script>
{%- endif -%}

{%- if request.page_type contains 'collection' -%}
  <script src="{{ 'jquery-ui.min.js' | asset_url }}" defer></script>
{%- endif -%}

{%- if content_for_layout contains 'class="parallax_block"' or content_for_layout contains 'class="parallax-scene"' -%}
  <script src="{{ 'parallax.min.js' | asset_url }}" defer></script>
{%- endif -%}


{%- if content_for_index contains 'index_section_slideshow__slide_video' -%}
    <script src="{{ 'YTPlayer.min.js' | asset_url }}" defer></script>
 {%- endif -%}

{%- if request.page_type contains 'blog' -%}
  <script src="{{ 'imagesLoaded.min.js' | asset_url }}" defer></script>
{%- endif -%}




{%- unless template == 'password' or template == 'page.password' -%}
    <script src="{{ 'header-script.js' | asset_url }}" defer></script>
{%- endunless -%}

<script src="{{ 'assets.js' | asset_url }}" defer></script>
<script src="{{ 'responsive.js' | asset_url }}" defer></script>
<script src="{{ 'shop.js' | asset_url }}" defer></script>

